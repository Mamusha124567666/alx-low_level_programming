#include "lists.h"
#include <stdio.h>
/**
*@len : length.
* @str: string
*/
size_t print_list(const list_t *h);

size_t node *h, *len, *str;
{
h->next = len;
len->next = str;
str->next = last;
last->next = NULL;
size_t node *temp = h;

if(temp==NULL)

{printf("[0] (nil)\n");
}
else
printf("[%d] %s\n", h->len, h->str);

return 0;
}
